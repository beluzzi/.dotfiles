#!/bin/bash
# Git fast commit

# check if git repo is 
if [ -z "$(git fetch --dry-run 2>&1)" ]; then
	echo "Warning: Local repo out of sync"
	return 1
fi


if [ $# -eq 0 ]; then
        echo "Error: You must provide a commit message."
        return 1
fi

git add .
git commit -m "$*"
git push
